version: 2.1

jobs:
  build:
    docker:
      - image: cimg/node:18.20.1-browsers  # updated Node + browsers image

    steps:
      - checkout

      # Install dependencies
      - run: npm install

      # Run tests
      - run: npm test

workflows:
  build_and_test:
    jobs:
      - build

